# AI-Native MMM 平台 · 产品说明

> 一句话：**把一次 8–12 周的营销组合建模（MMM）交付，重构成一条由 5 个智能体驱动、人类只在关键节点做裁决的流水线。**
> AI 不替你做决定，它把每一个决定都准备到"只差你点头"的程度。

---

## 文档地图

本文件是产品的**唯一入口文档**，面向客户、业务负责人与新加入的同事。想继续深潜，按需跳转：

| 想了解 | 去看 |
|---|---|
| 产品理念的完整论证（为什么固定流程也能 AI-Native） | `AI-Native-MMM-Value-Reframing.md` |
| 五智能体的详细设计规格 | `docs/agent-design/00-overview.md` ~ `05-*.md` |
| 双平面架构与 Proposal→Apply 协议 | `docs/agent-design/08-product-architecture-v2.md` |
| 交互与信息架构的重设计提案 | `MMM-Redesign-Cognition-First.md` |
| 工程实现与本地运行 | `CLAUDE.md` |

---

## 1. 我们解决什么问题

MMM 是一件**流程高度确定、但每一步都充满判断**的事。它的痛点从来不是"不知道下一步做什么"，而是：

| 传统交付的真实痛点 | 后果 |
|---|---|
| 业务理解靠人肉访谈 + Excel 因子树，口径散落在个人脑子里 | 换个人重做，结论就变 |
| 数据接入 60% 的工时花在对齐口径、清洗、反复回收 | 项目周期被数据拖垮 |
| 指标为什么被剔除，事后没人说得清 | 结论不可审计，客户不敢信 |
| 建模参数与假设写在脚本注释里 | 复现困难，交接昂贵 |
| 报告解读依赖资深顾问的手感 | 产能瓶颈锁死在少数几个人身上 |

**这不是流程自由度不够的问题，而是每个节点的"判断"没有被产品化。**

### 我们的重构

> 传统交付里，人做 100% 的执行 + 100% 的判断。
> 在这里，AI 做 100% 的执行 + 100% 的**预判断**，人只做**裁决**。

具体落地为三条产品原则：

**① Artifacts-Driven（交付物驱动）**
流程的每一步都产出一个**可查看、可编辑、可导出、可追溯**的交付物（Artifact），而不是一段聊天记录。项目档案、因子树、访谈提纲、数据请求、质量记分卡、主数据宽表、最终报告——共 21 个交付物，全部实体化。

**② AI 提 PR，人类 Review（不是聊天框，是工作台）**
AI 的每一次输出都是一份**带证据、带理由、带取舍说明的提案**，落在一张审阅卡上；人类的动作被压缩成"接受 / 改 / 拒"。工作单元不是对话轮次，是审阅卡。

**③ Transparent & Traceable（全链路可追溯）**
每个指标从进入到出局，都留下完整的裁决链条。**任何一层的否决，都被之后的每一层继承**——不会有指标被否了又悄悄回来。这条链条不是靠人工记账维护的，是从各层自己的记录里推导出来的，因此它永远不会与事实不符。

---

## 2. 产品全景

### 2.1 五个智能体 × 四个交付阶段

**智能体（Agent Roster）**

| 智能体 | 职责 | 核心能力 |
|---|---|---|
| **Project Control** | 流程编排与关卡 | 时序调度、Gate 管控、上下文同步 |
| **Business Agent** | 业务理解 | 项目定标、因子召回、访谈综合 |
| **Data Agent** | 数据接入与验证 | 质量评分、ETL、统计筛选 |
| **Model Agent** | 建模 | 先验约束、OLS MMM、模型校验 |
| **Report Agent** | 交付 | 效果分解、ROI、叙事撰写 |

**四个交付阶段（Stages）**

```
S1 业务理解  ──►  S2 数据接入与验证  ──►  S4 建模  ──►  S5 报告
里程碑:            里程碑:                里程碑:        里程碑:
数据请求确认       主数据锁定             最终模型选定    报告发布
```

全流程共 **37 个任务节点（S1 15 · S2 15 · S4 4 · S5 3）、21 个交付物**，DAG 依赖固定，构成产品的**蓝图（blueprint）**。

### 2.2 任务的四种协作类别

每个任务节点都被标注了它的"人机分工方式"——这是产品价值密度的核心设计：

| 类别 | 含义 | 谁在做 | 例子 |
|---|---|---|---|
| **M** | Delegate · 机械委派 | 确定性计算，AI 全自动 | OLS 拟合、主数据宽表装配、ASR 转写 |
| **A** | Augment · 增强 | AI 基于真实数据产出，人复核 | 数据质量四维评分、报告叙事撰写 |
| **C** | Assist · 协作 | AI 提案 + 强证据，人深度参与 | 因子树推导、访谈回写、项目档案抽取 |
| **H** | HITL · 人类关卡 | 人上传 / 人裁决，流程在此阻塞 | 所有 Gate、所有上传节点 |

**设计取舍**：我们不追求"全自动"。MMM 的价值恰恰在判断密集处，把判断交给 AI 才是失败模式。产品的目标是**让每一次人类判断的边际价值最大化**——把资料准备、口径对齐、证据聚合这些低价值动作全部吃掉，只把"这个指标该不该进模型"留给人。

### 2.3 两种运行模式

| 模式 | 行为 | 用途 |
|---|---|---|
| **Interactive（默认）** | 自动跑完所有 M/A/C 任务，**在每一个上传节点和裁决节点停下来**；人一提交，自动推进到下一个关卡 | 真实交付 |
| **Autopilot** | 自动采纳 AI 推荐、自动推进裁决 | 演示、回归验证 |

无论哪种模式，**S1 的三个上传关卡（SOW / 资料 / 访谈录音）都必须有真实文件才放行**——Autopilot 也只会跳过，绝不编造输入。**没有真实素材，就没有交付物。**

---

## 3. 分阶段能力

### S1 · 业务理解 — 把"资深顾问的脑子"变成可编辑的结构

目标：框定项目、锁定因子树、完成访谈、产出数据请求。

| 任务 | 类别 | AI 做什么 | 人做什么 |
|---|---|---|---|
| 1.0a 提供 SOW | H | — | 上传签署版 SOW 与立项简报 |
| 1.0 项目档案 | C | 从 SOW 抽取品牌 / 品类 / 市场 / 时间窗 / 目标，填入项目档案 | 确认并锁定分析粒度 |
| 1.1a 上传资料 | H | — | 上传品牌与竞品报告；并选择因子树基线来源（行业模板 / 上传自有因子树） |
| 1.1 行业知识包 | C | 召回适用的行业知识（L1/L2 骨架 + 品牌生意分析框架） | — |
| 1.21 推导因子树 | C | 在锁定的 L1/L2 上推导 L3/L4，为每个 L4 提出候选指标 + 三轴打分 | 可选择整体取向：谨慎 / 平衡 / 激进 |
| 1.21d 确认因子树 | H | — | 接受/调整 L3/L4，为每个 L4 选定主指标 |
| 1.3 访谈提纲 | A | 按受访层级（高管 / 管理 / 执行）生成结构化提纲，每题标注其探查的因子 | — |
| 1.3b **AI 预答** | C | **访谈前**先用已知信息把每道题答一遍，标注置信度与来源 | — |
| 1.4a 上传录音 | H | — | 上传访谈录音（音频）或文字纪要 |
| 1.4b ASR 转写 | M | 音频自动转写为文字 | — |
| 1.4 纪要回写 | C | 把纪要结构化为业务洞察 + 因子树修改建议，**每条都溯源到原话** | — |
| 1.4d 确认修改 | H | — | 逐条确认，接受的写回因子树 |
| 1.5 数据请求 | A | 生成数据采集模板：每个 L3 一个工作簿，每个 L4 一个 sheet，含粒度列、定义、示例行 | — |
| 1.5d 确认请求 | H | — | 与客户确认字段、粒度、责任人并签核 |
| 1.7 业务理解总结 | C | 汇总 S1 全部已确认交付物为一份收口摘要 | — |

**产品亮点 · AI 预答（1.3b）**
这是一个反直觉但极高价值的设计：AI **在访谈发生之前**就把提纲里的每个问题回答一遍。于是访谈的目的从"收集信息"变成"**验证与补缺**"——顾问带着一份有立场的草稿进会议室，而不是一张白纸。访谈时间被压缩，信息密度被提高。

**产品亮点 · Project Folder（项目文件夹）**
右下角常驻的资料坞，按类别（项目背景 / 行业参考 / 访谈纪要 / 因子树 / 数据）存放真实上传件。S1 的所有智能体**严格只以你上传的文件为依据**——没有任何演示数据兜底。这是"可信"的底线。

**产品亮点 · 交付物全可导出**
表格类导出 `.xlsx`，文档类导出 `.md`。所有 S1 交付物都能带走。

---

### S2 · 数据接入与验证 — 产品最重的一段

目标：引用准备好的数据资产，逐层验证，锁定主特征宽表。

这一阶段有 **6 个交付物、15 个任务**。**每一个交付物都不是一步生成的，而是一个"AI 提案 → 人类审阅 → 关卡放行"的完整 Process。**

```
2.1  数据处理         (H) ── 因子↔数据资产映射：AI 为每个未匹配因子推荐已发布指标，你接受/改绑/忽略
2.2  数据质量评分     (A) ── 10 项子检查 + AI 四维打分（一致性/完整性/粒度/准确性）
2.2d 审阅质量裁决     (H) ── 1=接受 · 0=不可用 · 0.5=人来定（重采 / 剔除 / 带告警接受）
2.3  业务校验         (C) ── 每个因子对照 sell-out 出图，AI 写读数
2.3a 解释异常         (A) ── 每个异常一张卡：AI 的因果假设 + 处理建议
2.3s 客户签核         (H) ── 逐因子签核；未签核的因子及其全部指标出局
2.4  统计评分         (A) ── CV / Pearson / VIF，合成入模得分 + AI 逐行论证
2.4d 审阅统计裁决     (H) ── 保留 Good 档，逐个决定 Acceptable 档，剔除 Unconsiderable 档
2.5  提出建模设置     (A) ── 提出 Y 候选、X 候选排序、默认变换与控制项
2.5y 确认响应变量     (H)
2.5x 审阅模型变量     (H) ── 每个变量带 Pearson r / VIF / CV / 2.4 裁决 + 剩余自由度
2.5p 确认模型设置     (H) ── adstock 滞留、饱和曲线、趋势与季节性控制
2.5r 拟合与复核       (M) ── 跑 OLS，逐因子 ROI/贡献对照知识库行业区间，越界项打旗
2.6  装配主数据       (M) ── 通过全部过滤层的指标 → 特征宽表
2.6d 锁定主数据       (H) ── 按 产品×渠道×区域 切片核对，人来锁
```

#### 核心机制 · 指标生命周期账本

S2 的脊梁。**六层过滤按顺序生效，任何一层的否决被之后的每一层继承**：

```
映射(2.1) → 质量(2.2d) → 业务签核(2.3) → 统计(2.4d) → 变量选择(2.5x) → 区间检验(2.5r)
```

被否的指标**不会被重新评分、不会被重新提供、永远进不了模型**。

这条设计有两个不显眼但关键的性质：

1. **账本不存储，只推导。** 每个指标的命运是从各层自己的记录（两张记分卡、签核结果、建模配置、区间裁决）现场推导出来的——所以它**不可能与事实不符**。
2. **继承不是记账，是统计正确性的要求。** VIF 是在整个变量集上计算的；如果已出局的指标还留在集合里，会虚高活着的指标的共线性。所以剔除死指标不是整洁，是正确。

**它带来的产品价值**：项目结束时，客户问"为什么小红书投放没进模型"，你打开主数据交付物，看到的是一条完整链条——"2.2 完整性 0.5 → 人工判定带告警接受 → 2.4 VIF 8.7 判定 Acceptable → 2.5x 因自由度不足未勾选"。**可审计，因此可信。**

#### 核心机制 · 异常裁决真的会改变模型

2.3a 的每张异常卡，你的裁决直接作用于拟合：

| 你的裁决 | 模型里发生什么 |
|---|---|
| **event**（结构性事件） | 在该时间窗生成一个哑变量控制项 |
| **cap**（异常值封顶） | 该处响应做 winsorize 处理 |
| **raw**（保持原样） | 数据不动，但一条 caveat 会带进最终报告 |

未处理或被拒的卡片不产生任何效果。**这里没有"记录了但没人读"的选项。**

#### 核心机制 · 2.6 主数据是可切片的视图

主数据交付物承载的是**漏斗 + 维度 + 每个指标的裁决链**；宽表本身按 产品 × 渠道 × 区域 实时查询。锁定是一个**人类动作**——建模永远不应该从一张没人看过的表上开始。

---

### S4 · 建模 — 假设先行，模型不自动选

| 任务 | 类别 | 内容 |
|---|---|---|
| 3.1 登记模型假设 | C | 把客户已确认的业务判断转成有界的模型约束，**每条溯源** |
| 3.2 训练与调优 | M | 按模型对象（渠道 × 区域组）训练，每个对象保留 3 个候选 |
| 3.3 技术复核 | C | 逐候选交叉核对 R² / 误差率 / 残差健康度；**基线为负 = 硬停** |
| 3.4 业务复核与选型 | H | 对照假设登记表，**由人选定最终模型** |

技术基准：R² 85–95% · MAPE 5–15% · DW 1.5–2.5。

**产品红线 · G4 永不自动选模。** 系统呈现通过技术复核的候选集及其取舍，选择优先级明确写在裁决卡上：① 是否符合业务判断 ② 效果分解是否合理 ③ 统计拟合优度。**顺序不能倒。** 一个 R² 最高但媒体贡献虚高的模型，不该因为数字漂亮就被选走。

**建模引擎**：自研轻依赖 OLS MMM —— adstock（滞留效应）+ Hill（饱和曲线）+ 回归，配趋势与 Fourier 季节性控制项。控制项吸收趋势与季节波动，付费变量才不会替它们背锅——这正是保证基线为正、系数符号正确的关键。

---

### S5 · 报告 — 数字来自计算，叙事来自 AI

| 任务 | 类别 | 内容 |
|---|---|---|
| 4.1a 构建图表 | M | 从选定模型生成效果分解、贡献度、增长归因、逐渠道 ROI |
| 4.1b 撰写叙事 | A | AI 按话术模板为每张图写读数；可选风格：高管精简 / 详尽论证 / 数据驱动 |
| 4.1c 复核与交付 | H | 终读，发布。**校验点：因子树里的每个问题都至少对应一张图** |

**产品红线 · 数字与叙事分离。**
所有指标数字由计算引擎产生，作为**权威事实**注入叙事提示词；AI 只负责组织语言。**叙事智能体不被允许发明任何数字。**

---

## 4. 支撑模块

### Data Engine（数据引擎）

一个独立的数据清洗与指标发布模块，取代了早期的"按槽位上传"方式，为 S2 供给结构化数据。

- **执行内核**：DuckDB + 真实 dbt 引擎。转换不是黑箱脚本，是可编译、可预览、可测试的 dbt 模型。
- **类型化转换步骤**：`field_map` · `enum_map` · `join` · `union` · `aggregate` · `filter` · `derive` · `custom_sql`。**AI 提议步骤，人在检查器里审阅参数**——不是纯自然语言指令，因为纯 NL 无法审计。
- **枚举映射是一等公民**：脏值到标准值的映射由 AI 建议（带置信度）、由人确认，编译成 dbt seed。渠道名、区域名的口径统一是 MMM 数据准备最耗时的环节，这里被产品化了。
- **逐表审阅 + 独立发布面**：清洗与发布是两个动作。发布后的指标才进入项目的指标目录，才可能被因子树映射引用。
- **版本化**：每个发布版本落 parquet，可回读、可回溯。

### Knowledge（知识库）

以**行业为第一维度**组织的知识包，可编辑、可跨项目复用：

| 模板种类 | 内容 |
|---|---|
| `factor_tree` | 行业因子树骨架（L1–L4 + 指标 + 预期 ROI/贡献区间） |
| `interview` | 分层访谈模板（Leadership / Management / Operation / Data） |
| `rules` | 校验规则库（质量 / 统计 / 技术 / 业务，分 block / warn / info 三档） |
| `industry_knowledge` | 行业专属知识 |
| `general_knowledge` | 跨行业通用知识 |

知识包可整体应用到项目（apply-pack），也可克隆后按客户定制。**行业经验 ROI/贡献区间就存在这里**——2.5r 的区间检验读的正是它。

### 多项目工作区

每个项目是一块**独立的黑板（blackboard）**：自己的状态文件、自己的文件夹、自己的运行守卫。项目之间可并发运行，互不干扰。项目在创建时绑定行业三级分类（L1/L2/L3），据此拉取对应知识包。

### 全局模型服务配置

LLM 与 ASR 的凭据在**全局设置**里配置一次，所有项目共用。凭据不进环境变量、不进代码库。未配置 LLM 时，运行按钮会引导你去设置页——不会静默失败。

---

## 5. 界面形态

不是聊天框，是一个**工作台**。左侧导航七个面：

| 面 | 作用 |
|---|---|
| **Project** | 主视图：阶段脊柱 + 交付物列 + 交付物详情（含各类专用编辑器） |
| **Workflow Canvas** | 全流程 DAG 画布，看清任务依赖与当前位置 |
| **Decisions** | 待办裁决收件箱，带未决计数徽标 |
| **Data Engine** | 数据资产、管线画布、步骤检查器、发布面 |
| **Assets** | 项目文件夹与数据请求清单 |
| **Knowledge** | 行业导航 + 知识包编辑 |
| **Settings** | 全局模型服务配置 |

**审阅卡（Review Card）是原子单元。** 每张卡携带：AI 的提案、支撑证据（可点开到源交付物）、推荐项与理由、每个选项的**后果说明**（选了会发生什么）。人的动作被压缩到一次点击。

**专用编辑器而非通用表格**：项目档案编辑器、因子树编辑器、质量记分卡编辑器、统计评分编辑器、OLS 面板（Y / X / 参数三组）、业务校验图表页——每种交付物有它自己的最优审阅形态。

---

## 6. 技术形态（一页概览）

```
前端  React 19 + Vite + TypeScript
      Zustand 状态 · 轮询后端 /state · 单一 API 面

后端  Python + FastAPI
      ├─ orchestrator/  DAG 引擎 + Autopilot 运行循环
      ├─ agents/        按智能体分组的任务处理器
      ├─ mmm/           OLS MMM 引擎（adstock / Hill / 回归 / 透视）
      ├─ dataeng/       DuckDB + dbt 数据引擎
      ├─ ingest/        真实文件解析（xlsx / xlsm / docx / pdf / pptx / csv）
      ├─ asr/           访谈音频转写
      └─ llm/           火山方舟（Volcano Ark）客户端

数据  每项目一份 JSON 状态 + parquet 数据资产 + 项目文件夹
```

**关键工程约定**（同时也是产品承诺）：
- 工作流是**配置**，内容是**计算**。任务 DAG、依赖、关卡、选项集是固定的产品蓝图；交付物内容与推荐**从不硬编码**，全部由确定性计算或有依据的 LLM 调用在运行时产生。
- 数字来自 `app/mmm`，不来自 LLM。
- 前后端类型严格对齐（Pydantic ↔ TypeScript），单一契约。

---

## 7. 一条最短使用路径

```
1. 建项目          填写项目名、品牌、行业三级分类
2. 配置模型服务    Settings 里填入 LLM 凭据（ASR 可选，用于访谈音频）
3. 上传 SOW        项目文件夹 → 项目背景     ▸ 1.0a 关卡放行
4. 上传资料        项目文件夹 → 行业参考     ▸ 1.1a 关卡放行
   └ 顺便选择因子树基线：用行业模板，还是上传自有因子树让 AI 补全
5. 审阅因子树      接受/调整 L3/L4 与主指标  ▸ 1.21d 关卡放行
6. 上传访谈        录音或纪要                ▸ 1.4a 关卡放行
   └ AI 已经预答过提纲了，此处是验证与补缺
7. 确认数据请求    导出模板给客户采集        ▸ S1 收口
8. Data Engine     导入回收的数据，清洗、映射枚举、发布指标
9. 走完 S2 六层    每层审阅一次，账本自动继承你的每一次否决
10. 选模型         对照假设登记表，人来定    ▸ S4 收口
11. 发布报告       终读，交付
```

**内置案例**：平台预置了一个真实客户案例——**达能脉动（Danone Mizone）中国市场 MMM**，项目 id `danone-mizone`，承载 23.8k 行真实建模数据、完整因子树、校验规则库与 12 份访谈纪要。它不是造出来的演示数据，是产品跑通端到端的真实基准；开启 Autopilot 约 8 分钟可走完全流程（耗时主要在 LLM 推理）。上手时建议先看这个案例，再建自己的项目。

---

## 8. 当前边界（我们明确不做的事）

诚实说明产品今天的位置：

| 边界 | 说明 |
|---|---|
| **参考数据集是单一客户案例专用** | 内置的参考数据加载器针对达能脉动（Danone Mizone）案例的文件格式编写——23.8k 行建模数据集、因子树、KBQ、校验规则、数据字典、12 份访谈纪要。无绑定数据的项目会回落到该参考集，而非报错。生产使用请走 Data Engine 路径。 |
| **建模变量上限 12** | 为保证 OLS 可识别（p < n），驱动变量取与 Y 相关性最高的前 12 个。宽表上传项目需注意。 |
| **响应变量必须由数据提供** | 数据请求模板不含 KPI（Y 是被解释变量，不是因子）。上传数据必须包含销量类指标，否则 S3–S5 无法拟合。 |
| **不做自动选模** | 见 S4 红线，这是设计选择而非缺失。 |
| **无多租户鉴权** | 当前面向单团队部署，未内置账号体系与权限模型。 |
| **建模引擎为 OLS 基线** | 贝叶斯 / 分层 / 时变系数等更高阶方法尚未接入。 |

---

## 9. 路线图（愿景与规划，尚未落地）

以下方向来自产品设计文档，代表我们认为这个产品应该长成的样子——**目前尚未在代码中实现**：

### 9.1 双平面架构 · Proposal → Apply 协议

把"AI 的提议"与"系统的状态"彻底分离成两个平面：

- **提案平面**：AI 的所有输出都是一个 Proposal 对象，带 diff、带证据、带置信度，可堆叠、可撤销、可对比。
- **状态平面**：只有被 Apply 的提案才改变系统状态；每次 Apply 都是一条不可变的账本条目。

价值：AI 可以大胆提议（哪怕错），因为提议本身不产生副作用；人可以并排比较多个提案再决定；任何状态变更都可精确回滚到某一次 Apply 之前。今天产品已经在 S2 局部体现了这个形态（审阅面板 + 裁决继承），目标是把它抽象成全局协议。

### 9.2 Insight Engine · 非线性挖掘

当前流程是严格线性的 DAG。但真正的洞察常常横向发生——"S2 发现的这个异常，和 S1 访谈里某位受访者的一句话，指向同一件事"。

规划中的 Insight Engine 在流程之外持续扫描全部交付物，主动提出跨阶段的关联假设，作为**非阻塞的旁路提示**出现，人可以采纳为正式任务，也可以忽略。**流程仍然固定，但认知不再被流程切断。**

### 9.3 Cognition-First 信息架构

当前界面以"流程进度"为主视角。重设计方向是翻转为**以认知负荷为主视角**：首屏回答的不是"跑到哪一步了"，而是"**现在需要我判断什么，为什么，依据是什么**"。流程进度退居辅助位。

配套地，为 AI 的认知产物设计一套专属视觉语言——让"这是 AI 的推断"与"这是计算出的事实"在视觉上一眼可辨，杜绝二者混淆。

### 9.4 Copilot 与流程驱动并存

除了沿流程推进，允许随时对任意交付物发起自由对话式追问（"这个 VIF 为什么这么高"、"把这个因子换成另一个指标会怎样"），Copilot 有全项目上下文，回答可直接转成一个提案。**流程负责保证不遗漏，Copilot 负责保证不受限。**

### 9.5 Change Ledger 迁入项目主视图

变更账本目前散落在各交付物内部。规划将其提升为项目级的一等对象：一条时间轴，记录谁在什么时候基于什么证据改变了什么，支持按指标、按因子、按人筛选。**这是交付物之外，第二份可以直接给客户的审计材料。**

### 9.6 更高阶的建模引擎

在 OLS 基线之上接入贝叶斯 MMM（先验可直接复用 3.1 的假设登记表）、分层模型（跨区域信息共享）、时变系数（媒体效率随时间漂移）。假设登记表的设计已经为此预留了接口形态——每条约束都是有界的、可溯源的，天然就是先验。

---

## 10. 术语表

| 术语 | 含义 |
|---|---|
| **MMM** | Marketing Mix Modeling，营销组合建模。用统计方法拆解各营销投入对销量的贡献与 ROI。 |
| **Artifact（交付物）** | 流程节点产出的可查看/编辑/导出/追溯的实体，共 22 个。 |
| **Stage（阶段）** | 四个交付阶段：S1 业务理解 / S2 数据接入与验证 / S4 建模 / S5 报告。 |
| **Task klass（任务类别）** | M 机械委派 / A 增强 / C 协作 / H 人类关卡。 |
| **Gate（关卡）** | 流程阻塞点，必须由人完成上传或裁决才放行。 |
| **HITL** | Human-In-The-Loop，人在回路。 |
| **Factor Tree（因子树）** | L1→L4 四级的业务因子结构，每个 L4 挂载候选指标。MMM 的业务骨架。 |
| **Factor Map（因子映射）** | 因子树的每一行 ↔ Data Engine 已发布指标 的绑定关系。 |
| **Indicator Ledger（指标生命周期账本）** | 六层过滤的裁决链条，逐层继承，全程可审计。 |
| **Master Data（主数据）** | 通过全部过滤层的指标装配成的特征宽表，建模的直接输入。 |
| **Adstock** | 广告的滞留/延续效应建模，本期投放对后续各期仍有衰减的影响。 |
| **Hill saturation** | 饱和曲线，刻画投放的边际效应递减。 |
| **VIF** | 方差膨胀因子，衡量变量间共线性程度。 |
| **CV** | 变异系数，衡量指标自身的波动性。指标太平稳则无解释力。 |
| **Model Object（模型对象）** | 一组独立拟合的单元，通常是 渠道 × 区域 的组合。 |
| **Data Asset（数据资产）** | Data Engine 中一份被清洗、版本化、可发布的数据表。 |
| **Knowledge Pack（知识包）** | 按行业组织的可复用模板集合（因子树 / 访谈 / 规则 / 知识）。 |

---

*本文档以当前代码实现为事实基准（`backend/app/domain/blueprint.py` 为工作流权威定义）。第 9 节明确标注为尚未落地的规划。*
